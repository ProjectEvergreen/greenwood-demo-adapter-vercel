diff --git a/node_modules/@greenwood/plugin-adapter-vercel/src/index.js b/node_modules/@greenwood/plugin-adapter-vercel/src/index.js
index 407ebc9..ad11d3d 100644
--- a/node_modules/@greenwood/plugin-adapter-vercel/src/index.js
+++ b/node_modules/@greenwood/plugin-adapter-vercel/src/index.js
@@ -13,7 +13,9 @@ function generateOutputFormat(id, type) {
     import { handler as ${handlerAlias} } from './${path}.js';
 
     export default async function handler (request, response) {
-      const { body, url, headers = {}, method } = request;
+      const { body, url, headers = {}, method, query } = request;
+      console.log({ request });
+      console.log({ query });
       const contentType = headers['content-type'] || '';
       let format = body;
 
@@ -40,7 +42,7 @@ function generateOutputFormat(id, type) {
         headers: new Headers(headers),
         method
       });
-      const res = await ${handlerAlias}(req);
+      const res = await ${handlerAlias}(req, { props: query });
 
       res.headers.forEach((value, key) => {
         response.setHeader(key, value);
@@ -79,18 +81,13 @@ async function vercelAdapter(compilation, options) {
   const adapterOutputUrl = new URL("./.vercel/output/functions/", projectDirectory);
   const ssrPages = compilation.graph.filter((page) => page.isSSR);
   const apiRoutes = compilation.manifest.apis;
+  const dynamicRoutes = [];
 
   if (await checkResourceExists(adapterOutputUrl)) {
     await fs.rm(adapterOutputUrl, { recursive: true });
   }
 
   await fs.mkdir(adapterOutputUrl, { recursive: true });
-  await fs.writeFile(
-    new URL("./.vercel/output/config.json", projectDirectory),
-    JSON.stringify({
-      version: 3,
-    }),
-  );
 
   for (const page of ssrPages) {
     const outputType = "page";
@@ -121,7 +118,8 @@ async function vercelAdapter(compilation, options) {
 
   for (const [key, value] of apiRoutes.entries()) {
     const outputType = "api";
-    const { id, outputHref, route } = apiRoutes.get(key);
+    const { id, outputHref, route, segment } = apiRoutes.get(key);
+    console.log({ segment });
     const outputRoot = new URL(`.${route}.func/`, adapterOutputUrl);
     const { assets = [] } = value;
 
@@ -134,8 +132,32 @@ async function vercelAdapter(compilation, options) {
 
       await fs.cp(new URL(asset), new URL(`./${name}`, outputRoot), { recursive: true });
     }
+
+    if(segment) {
+      const { key } = segment;
+      const basePath = route.replace(`[${key}]`, '');
+      console.log({ route });
+
+      dynamicRoutes.push({
+        // working example
+        // { "src": "/api/webhook/(?<type>[^/]+)", "dest": "/api/webhook/[type]" }
+        // "src": "/api/webhook/(?<type>[^/]+)",
+        // "dest": "/api/webhook/[type]"
+        src: `${basePath}(?<${key}>[^/]+)`,
+        dest: `${basePath}[${key}]`
+      })
+    }
   }
 
+  console.log({ dynamicRoutes });
+  await fs.writeFile(
+    new URL("./.vercel/output/config.json", projectDirectory),
+    JSON.stringify({
+      version: 3,
+      routes: dynamicRoutes
+    }),
+  );
+
   // static assets / build
   await fs.cp(outputDir, new URL("./.vercel/output/static/", projectDirectory), {
     recursive: true,
